// Copy this file to `config.local.js` (gitignored) and set your gateway address.
// The gateway listens on the sidecar port (default 8123) over your tailnet.
window.AGENT_URL = 'ws://YOUR-GATEWAY-HOST:8123';
